# API Server
PORT=3001
NODE_ENV=development

# MongoDB (configure when implementing database layer)
MONGODB_URI=mongodb://localhost:27017/stadium-gpt

# Google Gemini API (configure when implementing AI layer)
GEMINI_API_KEY=your_gemini_api_key_here

# Frontend (Vite — prefix with VITE_ for client exposure)
VITE_API_URL=http://localhost:3001
